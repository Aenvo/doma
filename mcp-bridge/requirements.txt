/**
 * DomA MCP: thin stdio client + singleton bridge daemon.
 *
 * - doma_mcp_stdio.py → stdio (per desktop agent)
 * - doma_bridge_daemon.py → HTTP :3846 + WS :3847 (one process)
 *
 * Env: DOMA_CONTROL_PORT (3846), DOMA_BRIDGE_PORT (3847), DOMA_BIND_TIMEOUT_SEC
 */
