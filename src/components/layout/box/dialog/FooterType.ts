export type FooterType = {
  cancelText?: string;
  cancelStyle?: object;
  cancelClass?: string;
  okText?: string;
  okStyle?: object;
  okClass?: string;
  btnDirection?: string; // 按钮方向，默认水平排列, 可选值：horizontal, vertical
  btnPlacement?: string; // 按钮位置，默认在水平居中, 可选值：left, right, center
};